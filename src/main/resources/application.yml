spring:
  application:
    name: echo
management:
  endpoints:
    web:
      exposure:
        include: "*"
eureka:
  instance:
    status-page-url-path: http://localhost:8080/actuator/health
    instance-id: echo-application
  client:
    serviceUrl:
      defaultZone: http://discovery:8761/eureka/
